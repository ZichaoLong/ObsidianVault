加速定理

结构的普遍存在性

压缩即智能，压缩即学习结构

还有一些别的研究，

少量可随机读写的RAM + 无限只能写入一次但可随机读的Write\-once Memory，它与RAM/RASP的Gap是多少？

**结论**🤜\[6\-m\]：RAM上执行步数$t$，消耗空间$s$的算法，在这类图灵机上可使用$t\cdot\log(s)$时间模拟。



这一结论中，少量的读写RAM主要用于记录当前状态和动态地址簿，当有无限的可随机读的Write\-Once Memory时，性能与RAM并不会相差很多，这实际上启发我们

能否通过Agent让能力较差的模型，提升能力上限？

Agent的导航memory方案：既具备无限迭代的可能性，又解决了kv cache无限append的问题，似乎只有一个缺陷，每次要重新读或者改。这是当前指令集还不能支撑情况下的必然。
